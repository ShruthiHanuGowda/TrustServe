import React, {
    useEffect,
    useMemo,
    useState,
} from 'react';

import {
    SafeAreaView,
    View,
    Text,
    StyleSheet,
    Image,
    TouchableOpacity,
    FlatList,
    ActivityIndicator,
    Alert,
} from 'react-native';

import {
    useQuery,
    useMutation,
} from '@apollo/client';

import { useUser } from '../../../context/UserContext';

import {
    GET_SALON,
    LIST_SERVICES,
    ADD_FAVORITE_SALON,
    REMOVE_FAVORITE_SALON,
    IS_FAVORITE_SALON,
} from '../../../graphql/queries';

const PRIMARY = '#009D94';

type Props = {
    navigation: any;
    route: any;
};

type Service = {
    serviceId: string;
    salonId: string;
    name: string;
    category: string;
    description?: string;
    duration: number;
    price: number;
    gender: string;
    popular: boolean;
    active: boolean;
};

type BusinessDay = {
    open?: string;
    close?: string;
    isOpen?: boolean;
};

type BusinessHours = {
    MONDAY?: BusinessDay;
    TUESDAY?: BusinessDay;
    WEDNESDAY?: BusinessDay;
    THURSDAY?: BusinessDay;
    FRIDAY?: BusinessDay;
    SATURDAY?: BusinessDay;
    SUNDAY?: BusinessDay;
};

/**
 * ----------------------------------------------------
 * SALON MEDIA
 * ----------------------------------------------------
 *
 * Permanent S3 key is stored in `key`.
 *
 * `objectUrl` is expected to be a fresh signed
 * URL generated by Lambda.
 */
type SalonMedia = {
    imageId?: string;
    salonId?: string;
    mediaType?: string;
    key?: string;
    objectUrl?: string | null;
    status?: string;
    uploadedAt?: string;
    approvedAt?: string;
    approvedBy?: string;
    rejectedAt?: string;
    rejectedBy?: string;
    rejectionReason?: string;
};

type Salon = {
    salonId: string;
    salonName: string;
    ownerName?: string;
    businessType?: string;

    address?: {
        addressLine?: string;
        city?: string;
        state?: string;
        pincode?: string;
    };

    /**
     * Legacy/direct URL fields.
     *
     * These are kept in the type because the API may still
     * return them, but approved media below is preferred.
     */
    logoUrl?: string;
    coverImageUrl?: string;
    galleryImages?: string[];

    /**
     * ----------------------------------------------------
     * APPROVED MEDIA
     * ----------------------------------------------------
     */
    logoMedia?: SalonMedia | null;
    coverMedia?: SalonMedia | null;
    galleryMedia?: SalonMedia[];

    averageRating?: number;
    totalReviews?: number;

    salonStatus?: string;

    /**
     * Some API responses may return:
     *
     * salon.MONDAY
     *
     * while others may return:
     *
     * salon.businessHours.MONDAY
     *
     * Support both.
     */
    MONDAY?: BusinessDay;
    TUESDAY?: BusinessDay;
    WEDNESDAY?: BusinessDay;
    THURSDAY?: BusinessDay;
    FRIDAY?: BusinessDay;
    SATURDAY?: BusinessDay;
    SUNDAY?: BusinessDay;

    businessHours?: BusinessHours;
};

type Offer = {
    offerId: string;
    salonId: string;
    salonName?: string;
    title: string;
    description?: string;

    discountType:
    | 'PERCENTAGE'
    | 'FIXED'
    | string;

    discountValue: number;

    couponCode?: string | null;

    minimumBookingAmount?:
    | number
    | null;

    category?: string | null;

    serviceIds: string[];

    startDate?: string;
    endDate?: string;

    status?: string;
};

export default function SalonDetailsScreen({
    navigation,
    route,
}: Props) {
    const { currentUser } = useUser();

    const salonId =
        route.params?.salonId;

    /**
     * ----------------------------------------------------
     * OFFER FROM PREVIOUS SCREEN
     * ----------------------------------------------------
     */
    const routeOffer =
        route.params?.offer ?? null;

    const routeOfferId =
        route.params?.offerId ??
        routeOffer?.offerId ??
        routeOffer?.id ??
        null;

    /**
     * ----------------------------------------------------
     * SELECTED SERVICES
     * ----------------------------------------------------
     */
    const [selectedServices, setSelectedServices] =
        useState<Service[]>([]);

    /**
     * ----------------------------------------------------
     * FAVORITE LOCAL STATE
     * ----------------------------------------------------
     */
    const [isFavorite, setIsFavorite] =
        useState(false);

    const [favoriteLoading, setFavoriteLoading] =
        useState(false);

    /**
     * ----------------------------------------------------
     * MEDIA LOAD STATES
     * ----------------------------------------------------
     */
    const [coverLoadFailed, setCoverLoadFailed] =
        useState(false);

    const [galleryLoadFailed, setGalleryLoadFailed] =
        useState<Record<string, boolean>>({});

    /**
     * ----------------------------------------------------
     * GET SALON
     * ----------------------------------------------------
     */
    const {
        data: salonData,
        loading: salonLoading,
        error: salonError,
    } = useQuery(GET_SALON, {
        variables: {
            salonId,
        },
        skip: !salonId,
        fetchPolicy: 'network-only',
    });

    /**
     * ----------------------------------------------------
     * GET SERVICES
     * ----------------------------------------------------
     */
    const {
        data: servicesData,
        loading: servicesLoading,
        error: servicesError,
    } = useQuery(LIST_SERVICES, {
        variables: {
            salonId,
        },
        skip: !salonId,
        fetchPolicy: 'network-only',
    });

    const salon: Salon | null =
        salonData?.getSalon ?? null;

    const services: Service[] = (
        servicesData?.listServices ?? []
    ).filter(
        (service: Service) =>
            service.active,
    );

    /**
     * ----------------------------------------------------
     * APPROVED COVER MEDIA
     * ----------------------------------------------------
     *
     * IMPORTANT:
     *
     * Do NOT fall back to logoUrl or picsum.
     *
     * The backend is expected to generate a fresh
     * signed S3 objectUrl.
     */
    const approvedCover =
        salon?.coverMedia?.status ===
            'APPROVED' &&
            salon?.coverMedia?.objectUrl
            ? salon.coverMedia.objectUrl
            : null;

    /**
     * ----------------------------------------------------
     * APPROVED GALLERY MEDIA
     * ----------------------------------------------------
     *
     * Only approved gallery media with a valid
     * signed objectUrl is displayed.
     */
    const approvedGallery =
        useMemo(() => {
            if (
                !Array.isArray(
                    salon?.galleryMedia,
                )
            ) {
                return [];
            }

            return salon.galleryMedia.filter(
                media =>
                    String(
                        media?.status || '',
                    ).toUpperCase() ===
                    'APPROVED' &&
                    Boolean(
                        media?.objectUrl,
                    ),
            );
        }, [
            salon?.galleryMedia,
        ]);


    console.log('[SalonDetails] GALLERY MEDIA:', {
        salonId: salon?.salonId,
        galleryMedia: salon?.galleryMedia,
        approvedGallery,
    });
    /**
     * ----------------------------------------------------
     * RESET MEDIA LOAD STATES
     * ----------------------------------------------------
     */
    useEffect(() => {
        setCoverLoadFailed(false);
        setGalleryLoadFailed({});
    }, [
        salon?.salonId,
        approvedCover,
        approvedGallery,
    ]);

    /**
     * ----------------------------------------------------
     * NORMALIZE OFFER
     * ----------------------------------------------------
     */
    const normalizedOffer:
        | Offer
        | null = useMemo(() => {
            if (!routeOffer) {
                return null;
            }

            return {
                ...routeOffer,

                discountValue: Number(
                    routeOffer.discountValue || 0,
                ),

                minimumBookingAmount:
                    routeOffer.minimumBookingAmount !==
                        null &&
                        routeOffer.minimumBookingAmount !==
                        undefined
                        ? Number(
                            routeOffer.minimumBookingAmount,
                        )
                        : null,

                serviceIds:
                    Array.isArray(
                        routeOffer.serviceIds,
                    )
                        ? routeOffer.serviceIds
                        : [],
            };
        }, [routeOffer]);

    /**
     * ----------------------------------------------------
     * OFFER VALIDITY
     * ----------------------------------------------------
     */
    const isOfferValid =
        useMemo(() => {
            if (!normalizedOffer) {
                return false;
            }

            if (
                normalizedOffer.status &&
                String(
                    normalizedOffer.status,
                ).toUpperCase() !== 'ACTIVE'
            ) {
                return false;
            }

            const now = new Date();

            if (
                normalizedOffer.startDate
            ) {
                const startDate =
                    new Date(
                        normalizedOffer.startDate,
                    );

                if (
                    !Number.isNaN(
                        startDate.getTime(),
                    ) &&
                    now < startDate
                ) {
                    return false;
                }
            }

            if (
                normalizedOffer.endDate
            ) {
                const endDate =
                    new Date(
                        normalizedOffer.endDate,
                    );

                if (
                    !Number.isNaN(
                        endDate.getTime(),
                    ) &&
                    now > endDate
                ) {
                    return false;
                }
            }

            return true;
        }, [normalizedOffer]);

    /**
     * ----------------------------------------------------
     * CHECK SERVICE ELIGIBILITY
     * ----------------------------------------------------
     *
     * Priority:
     *
     * 1. serviceIds
     * 2. category
     * 3. all services
     */
    const isServiceEligibleForOffer = (
        service: Service,
    ): boolean => {
        if (
            !normalizedOffer ||
            !isOfferValid
        ) {
            return false;
        }

        const serviceIds =
            normalizedOffer.serviceIds ||
            [];

        if (
            serviceIds.length > 0
        ) {
            return serviceIds.includes(
                service.serviceId,
            );
        }

        if (
            normalizedOffer.category &&
            normalizedOffer.category.trim()
        ) {
            return (
                String(
                    service.category || '',
                )
                    .trim()
                    .toLowerCase() ===
                String(
                    normalizedOffer.category,
                )
                    .trim()
                    .toLowerCase()
            );
        }

        return true;
    };

    /**
     * ----------------------------------------------------
     * FAVORITE STATUS
     * ----------------------------------------------------
     */
    const {
        data: favoriteData,
        loading:
        favoriteStatusLoading,
        refetch:
        refetchFavoriteStatus,
    } = useQuery(
        IS_FAVORITE_SALON,
        {
            variables: {
                userId:
                    currentUser?.userId ??
                    '',
                salonId,
            },

            skip:
                !currentUser?.userId ||
                !salonId,

            fetchPolicy:
                'network-only',

            notifyOnNetworkStatusChange:
                true,
        },
    );

    /**
     * ----------------------------------------------------
     * SYNC FAVORITE STATE
     * ----------------------------------------------------
     */
    useEffect(() => {
        if (
            favoriteData &&
            favoriteData.isFavoriteSalon !==
            undefined
        ) {
            setIsFavorite(
                favoriteData.isFavoriteSalon ===
                true,
            );
        }
    }, [favoriteData]);

    /**
     * ----------------------------------------------------
     * FAVORITE MUTATIONS
     * ----------------------------------------------------
     */
    const [addFavorite] =
        useMutation(
            ADD_FAVORITE_SALON,
        );

    const [removeFavorite] =
        useMutation(
            REMOVE_FAVORITE_SALON,
        );

    /**
     * ----------------------------------------------------
     * TOGGLE FAVORITE
     * ----------------------------------------------------
     */
    const handleFavorite =
        async () => {
            if (
                !currentUser?.userId
            ) {
                Alert.alert(
                    'Login required',
                    'Please login to add salons to your favorites.',
                );

                return;
            }

            if (!salonId) {
                console.log(
                    'Salon ID is missing',
                );

                return;
            }

            if (favoriteLoading) {
                return;
            }

            const previousState =
                isFavorite;

            try {
                setFavoriteLoading(
                    true,
                );

                /**
                 * Optimistic UI.
                 */
                setIsFavorite(
                    !previousState,
                );

                /**
                 * REMOVE
                 */
                if (
                    previousState
                ) {
                    const { data } =
                        await removeFavorite(
                            {
                                variables: {
                                    input: {
                                        userId:
                                            currentUser.userId,

                                        salonId,
                                    },
                                },
                            },
                        );

                    console.log(
                        'Remove favorite response:',
                        data?.removeFavoriteSalon,
                    );

                    if (
                        !data
                            ?.removeFavoriteSalon
                            ?.success
                    ) {
                        setIsFavorite(
                            previousState,
                        );

                        console.log(
                            'Remove favorite failed:',
                            data
                                ?.removeFavoriteSalon
                                ?.message,
                        );
                    }
                }

                /**
                 * ADD
                 */
                else {
                    const { data } =
                        await addFavorite(
                            {
                                variables: {
                                    input: {
                                        userId:
                                            currentUser.userId,

                                        salonId,
                                    },
                                },
                            },
                        );

                    console.log(
                        'Add favorite response:',
                        data?.addFavoriteSalon,
                    );

                    if (
                        !data
                            ?.addFavoriteSalon
                            ?.success
                    ) {
                        setIsFavorite(
                            previousState,
                        );

                        console.log(
                            'Add favorite failed:',
                            data
                                ?.addFavoriteSalon
                                ?.message,
                        );
                    }
                }

                await refetchFavoriteStatus();
            } catch (error) {
                console.error(
                    'Favorite salon error:',
                    error,
                );

                /**
                 * Rollback to the actual
                 * previous state.
                 */
                setIsFavorite(
                    previousState,
                );
            } finally {
                setFavoriteLoading(
                    false,
                );
            }
        };

    /**
     * ----------------------------------------------------
     * CATEGORIES
     * ----------------------------------------------------
     */
    const categories =
        Array.from(
            new Set(
                services
                    .map(
                        service =>
                            service.category,
                    )
                    .filter(Boolean),
            ),
        );

    /**
     * ----------------------------------------------------
     * ORIGINAL SELECTED SERVICES SUBTOTAL
     * ----------------------------------------------------
     */
    const subtotal =
        useMemo(() => {
            return selectedServices.reduce(
                (
                    sum,
                    item,
                ) =>
                    sum +
                    Number(
                        item.price || 0,
                    ),
                0,
            );
        }, [selectedServices]);

    /**
     * ----------------------------------------------------
     * OFFER ELIGIBLE SUBTOTAL
     * ----------------------------------------------------
     */
    const eligibleSubtotal =
        useMemo(() => {
            return selectedServices.reduce(
                (
                    sum,
                    item,
                ) => {
                    if (
                        !isServiceEligibleForOffer(
                            item,
                        )
                    ) {
                        return sum;
                    }

                    return (
                        sum +
                        Number(
                            item.price || 0,
                        )
                    );
                },
                0,
            );
        }, [
            selectedServices,
            normalizedOffer,
            isOfferValid,
        ]);

    /**
     * ----------------------------------------------------
     * MINIMUM BOOKING AMOUNT
     * ----------------------------------------------------
     */
    const minimumBookingAmountMet =
        useMemo(() => {
            if (
                !normalizedOffer ||
                normalizedOffer.minimumBookingAmount ===
                null ||
                normalizedOffer.minimumBookingAmount ===
                undefined
            ) {
                return true;
            }

            return (
                subtotal >=
                Number(
                    normalizedOffer.minimumBookingAmount,
                )
            );
        }, [
            normalizedOffer,
            subtotal,
        ]);

    /**
     * ----------------------------------------------------
     * DISCOUNT AMOUNT
     * ----------------------------------------------------
     */
    const discountAmount =
        useMemo(() => {
            if (
                !normalizedOffer ||
                !isOfferValid ||
                !minimumBookingAmountMet ||
                eligibleSubtotal <= 0
            ) {
                return 0;
            }

            const discountValue =
                Number(
                    normalizedOffer.discountValue ||
                    0,
                );

            if (
                discountValue <= 0
            ) {
                return 0;
            }

            /**
             * PERCENTAGE
             */
            if (
                String(
                    normalizedOffer.discountType,
                ).toUpperCase() ===
                'PERCENTAGE'
            ) {
                return Math.min(
                    eligibleSubtotal,
                    (
                        eligibleSubtotal *
                        discountValue
                    ) / 100,
                );
            }

            /**
             * FIXED
             */
            if (
                String(
                    normalizedOffer.discountType,
                ).toUpperCase() ===
                'FIXED'
            ) {
                return Math.min(
                    eligibleSubtotal,
                    discountValue,
                );
            }

            return 0;
        }, [
            normalizedOffer,
            isOfferValid,
            minimumBookingAmountMet,
            eligibleSubtotal,
        ]);

    /**
     * ----------------------------------------------------
     * FINAL DISPLAY TOTAL
     * ----------------------------------------------------
     */
    const discountedServicesTotal =
        useMemo(() => {
            return Math.max(
                0,
                subtotal -
                discountAmount,
            );
        }, [
            subtotal,
            discountAmount,
        ]);

    const offerApplied =
        Boolean(
            normalizedOffer &&
            isOfferValid &&
            minimumBookingAmountMet &&
            discountAmount > 0,
        );

    /**
     * ----------------------------------------------------
     * SERVICE DISPLAY PRICE
     * ----------------------------------------------------
     */
    const getServiceDisplayPrice =
        (
            service: Service,
        ): number => {
            const originalPrice =
                Number(
                    service.price || 0,
                );

            if (
                !offerApplied ||
                !isServiceEligibleForOffer(
                    service,
                )
            ) {
                return originalPrice;
            }

            const discountType =
                String(
                    normalizedOffer?.discountType ||
                    '',
                ).toUpperCase();

            /**
             * Percentage offer.
             */
            if (
                discountType ===
                'PERCENTAGE'
            ) {
                const percentage =
                    Number(
                        normalizedOffer?.discountValue ||
                        0,
                    );

                const serviceDiscount =
                    (
                        originalPrice *
                        percentage
                    ) / 100;

                return Math.max(
                    0,
                    originalPrice -
                    serviceDiscount,
                );
            }

            /**
             * Fixed offer.
             */
            if (
                discountType ===
                'FIXED' &&
                eligibleSubtotal >
                0
            ) {
                const serviceShare =
                    originalPrice /
                    eligibleSubtotal;

                const allocatedDiscount =
                    discountAmount *
                    serviceShare;

                return Math.max(
                    0,
                    originalPrice -
                    allocatedDiscount,
                );
            }

            return originalPrice;
        };

    /**
     * ----------------------------------------------------
     * OFFER DISCOUNT TEXT
     * ----------------------------------------------------
     */
    const getOfferDiscountText =
        () => {
            if (
                !normalizedOffer
            ) {
                return '';
            }

            if (
                String(
                    normalizedOffer.discountType,
                ).toUpperCase() ===
                'PERCENTAGE'
            ) {
                return `${normalizedOffer.discountValue}% OFF`;
            }

            if (
                String(
                    normalizedOffer.discountType,
                ).toUpperCase() ===
                'FIXED'
            ) {
                return `₹${normalizedOffer.discountValue} OFF`;
            }

            return 'Offer Applied';
        };

    /**
     * ----------------------------------------------------
     * OFFER MESSAGE
     * ----------------------------------------------------
     */
    const offerMessage =
        useMemo(() => {
            if (
                !normalizedOffer
            ) {
                return null;
            }

            if (
                !isOfferValid
            ) {
                return 'This offer is no longer active.';
            }

            if (
                !minimumBookingAmountMet &&
                normalizedOffer.minimumBookingAmount !==
                null &&
                normalizedOffer.minimumBookingAmount !==
                undefined
            ) {
                const remaining =
                    Math.max(
                        0,
                        Number(
                            normalizedOffer.minimumBookingAmount,
                        ) -
                        subtotal,
                    );

                return `Add ₹${remaining.toFixed(
                    0,
                )} more to use this offer.`;
            }

            if (
                offerApplied
            ) {
                return `You save ₹${discountAmount.toFixed(
                    0,
                )} with this offer.`;
            }

            return null;
        }, [
            normalizedOffer,
            isOfferValid,
            minimumBookingAmountMet,
            subtotal,
            offerApplied,
            discountAmount,
        ]);

    /**
     * ----------------------------------------------------
     * TOTAL DURATION
     * ----------------------------------------------------
     */
    const totalDuration =
        selectedServices.reduce(
            (
                sum,
                item,
            ) =>
                sum +
                Number(
                    item.duration || 0,
                ),
            0,
        );

    /**
     * ----------------------------------------------------
     * BUSINESS HOURS HELPERS
     * ----------------------------------------------------
     */
    const getBusinessDay =
        (
            day:
                | keyof BusinessHours,
        ): BusinessDay | undefined => {
            if (!salon) {
                return undefined;
            }

            const directDay =
                (
                    salon as any
                )?.[day];

            if (
                directDay &&
                typeof directDay ===
                'object'
            ) {
                return directDay;
            }

            const nestedDay =
                salon.businessHours?.[
                day
                ];

            if (
                nestedDay &&
                typeof nestedDay ===
                'object'
            ) {
                return nestedDay;
            }

            return undefined;
        };

    /**
     * ----------------------------------------------------
     * GET CURRENT DAY
     * ----------------------------------------------------
     */
    const getCurrentDay =
        (): keyof BusinessHours => {
            const day =
                new Date().getDay();

            const days: (
                keyof BusinessHours
            )[] = [
                    'SUNDAY',
                    'MONDAY',
                    'TUESDAY',
                    'WEDNESDAY',
                    'THURSDAY',
                    'FRIDAY',
                    'SATURDAY',
                ];

            return days[day];
        };

    const today =
        getCurrentDay();

    const todayHours =
        getBusinessDay(
            today,
        );

    /**
     * ----------------------------------------------------
     * CONVERT HH:mm TO MINUTES
     * ----------------------------------------------------
     */
    const parseTimeToMinutes =
        (
            time?: string,
        ): number => {
            if (!time) {
                return NaN;
            }

            const parts =
                String(time).split(
                    ':',
                );

            const hours =
                Number(
                    parts[0],
                );

            const minutes =
                Number(
                    parts[1] || 0,
                );

            if (
                Number.isNaN(
                    hours,
                ) ||
                Number.isNaN(
                    minutes,
                )
            ) {
                return NaN;
            }

            if (
                hours === 24
            ) {
                return 24 * 60;
            }

            return (
                hours * 60 +
                minutes
            );
        };

    /**
     * ----------------------------------------------------
     * CURRENT OPEN/CLOSED STATUS
     * ----------------------------------------------------
     */
    const getIsSalonOpen =
        (): boolean => {
            if (!todayHours) {
                return false;
            }

            if (
                todayHours.isOpen !==
                true
            ) {
                return false;
            }

            if (
                !todayHours.open ||
                !todayHours.close
            ) {
                return false;
            }

            const now =
                new Date();

            const currentMinutes =
                now.getHours() *
                60 +
                now.getMinutes();

            const openMinutes =
                parseTimeToMinutes(
                    todayHours.open,
                );

            const closeMinutes =
                parseTimeToMinutes(
                    todayHours.close,
                );

            if (
                Number.isNaN(
                    openMinutes,
                ) ||
                Number.isNaN(
                    closeMinutes,
                )
            ) {
                return false;
            }

            /**
             * Normal schedule.
             */
            if (
                closeMinutes >
                openMinutes
            ) {
                return (
                    currentMinutes >=
                    openMinutes &&
                    currentMinutes <
                    closeMinutes
                );
            }

            /**
             * Overnight schedule.
             */
            if (
                closeMinutes <
                openMinutes
            ) {
                return (
                    currentMinutes >=
                    openMinutes ||
                    currentMinutes <
                    closeMinutes
                );
            }

            /**
             * Same open/close =
             * closed.
             */
            return false;
        };

    const isOpen =
        getIsSalonOpen();

    /**
     * ----------------------------------------------------
     * TOGGLE SERVICE
     * ----------------------------------------------------
     */
    const toggleService =
        (
            service: Service,
        ) => {
            const exists =
                selectedServices.some(
                    item =>
                        item.serviceId ===
                        service.serviceId,
                );

            if (exists) {
                setSelectedServices(
                    prev =>
                        prev.filter(
                            item =>
                                item.serviceId !==
                                service.serviceId,
                        ),
                );
            } else {
                setSelectedServices(
                    prev => [
                        ...prev,
                        service,
                    ],
                );
            }
        };

    /**
     * ----------------------------------------------------
     * ADDRESS
     * ----------------------------------------------------
     */
    const address = [
        salon?.address
            ?.addressLine,

        salon?.address?.city,

        salon?.address?.state,
    ]
        .filter(Boolean)
        .join(', ');

    /**
     * ----------------------------------------------------
     * DEBUG
     * ----------------------------------------------------
     */
    console.log(
        '======================================',
    );

    console.log(
        'SALON DETAILS',
    );

    console.log(
        'Salon:',
        salon,
    );

    console.log(
        'Salon ID:',
        salon?.salonId,
    );

    console.log(
        'Salon Name:',
        salon?.salonName,
    );

    console.log(
        'Approved Cover:',
        approvedCover,
    );

    console.log(
        'Approved Gallery:',
        approvedGallery,
    );

    console.log(
        'Today:',
        today,
    );

    console.log(
        'Today Business Hours:',
        todayHours,
    );

    console.log(
        'Salon Currently Open:',
        isOpen,
    );

    console.log(
        'Services:',
        services,
    );

    console.log(
        'Offer from route:',
        routeOffer,
    );

    console.log(
        'Offer ID:',
        routeOfferId,
    );

    console.log(
        'Selected Services:',
        selectedServices,
    );

    console.log(
        'Subtotal:',
        subtotal,
    );

    console.log(
        'Eligible Subtotal:',
        eligibleSubtotal,
    );

    console.log(
        'Discount:',
        discountAmount,
    );

    console.log(
        'Offer Applied:',
        offerApplied,
    );

    console.log(
        'Discounted Total:',
        discountedServicesTotal,
    );

    console.log(
        'Favorite:',
        isFavorite,
    );

    console.log(
        '======================================',
    );

    /**
     * ----------------------------------------------------
     * LOADING
     * ----------------------------------------------------
     */
    if (
        salonLoading ||
        servicesLoading
    ) {
        return (
            <SafeAreaView
                style={
                    styles.loadingContainer
                }
            >
                <ActivityIndicator
                    size="large"
                    color={PRIMARY}
                />

                <Text
                    style={
                        styles.loadingText
                    }
                >
                    Loading salon...
                </Text>
            </SafeAreaView>
        );
    }

    /**
     * ----------------------------------------------------
     * ERROR
     * ----------------------------------------------------
     */
    if (
        salonError ||
        servicesError
    ) {
        console.log(
            'Salon Error:',
            salonError,
        );

        console.log(
            'Services Error:',
            servicesError,
        );

        return (
            <SafeAreaView
                style={
                    styles.loadingContainer
                }
            >
                <Text
                    style={
                        styles.errorTitle
                    }
                >
                    Unable to load salon
                </Text>

                <Text
                    style={
                        styles.errorText
                    }
                >
                    Please check your internet
                    connection and try again.
                </Text>

                <TouchableOpacity
                    style={
                        styles.retryButton
                    }
                    onPress={() =>
                        navigation.goBack()
                    }
                >
                    <Text
                        style={
                            styles.retryText
                        }
                    >
                        Go Back
                    </Text>
                </TouchableOpacity>
            </SafeAreaView>
        );
    }

    /**
     * ----------------------------------------------------
     * SALON NOT FOUND
     * ----------------------------------------------------
     */
    if (!salon) {
        return (
            <SafeAreaView
                style={
                    styles.loadingContainer
                }
            >
                <Text
                    style={
                        styles.errorTitle
                    }
                >
                    Salon not found
                </Text>

                <Text
                    style={
                        styles.errorText
                    }
                >
                    This salon may no longer be
                    available.
                </Text>

                <TouchableOpacity
                    style={
                        styles.retryButton
                    }
                    onPress={() =>
                        navigation.goBack()
                    }
                >
                    <Text
                        style={
                            styles.retryText
                        }
                    >
                        Go Back
                    </Text>
                </TouchableOpacity>
            </SafeAreaView>
        );
    }

    return (
        <SafeAreaView
            style={styles.container}
        >
            <FlatList
                data={services}
                keyExtractor={item =>
                    item.serviceId
                }
                showsVerticalScrollIndicator={
                    false
                }
                contentContainerStyle={{
                    paddingBottom:
                        selectedServices.length >
                            0
                            ? 145
                            : 30,
                }}
                ListHeaderComponent={
                    <>
                        {/* HEADER */}

                        <View
                            style={
                                styles.header
                            }
                        >
                            <TouchableOpacity
                                style={
                                    styles.headerButton
                                }
                                onPress={() =>
                                    navigation.goBack()
                                }
                            >
                                <Text
                                    style={
                                        styles.back
                                    }
                                >
                                    ‹
                                </Text>
                            </TouchableOpacity>

                            <Text
                                style={
                                    styles.headerTitle
                                }
                                numberOfLines={
                                    1
                                }
                            >
                                {
                                    salon.salonName
                                }
                            </Text>

                            <TouchableOpacity
                                style={
                                    styles.headerButton
                                }
                                activeOpacity={
                                    0.8
                                }
                                disabled={
                                    favoriteLoading ||
                                    favoriteStatusLoading
                                }
                                onPress={
                                    handleFavorite
                                }
                            >
                                <Text
                                    style={[
                                        styles.favorite,
                                        isFavorite &&
                                        styles.favoriteActive,
                                    ]}
                                >
                                    {isFavorite
                                        ? '♥'
                                        : '♡'}
                                </Text>
                            </TouchableOpacity>
                        </View>

                        {/* ------------------------------------------------
                            COVER PHOTO
                        ------------------------------------------------ */}

                        {approvedCover &&
                            !coverLoadFailed ? (
                            <Image
                                source={{
                                    uri:
                                        approvedCover,
                                }}
                                style={
                                    styles.cover
                                }
                                resizeMode="cover"
                                onLoadStart={() => {
                                    console.log(
                                        '[SalonDetails] Cover image loading:',
                                        {
                                            salonId:
                                                salon.salonId,
                                            url:
                                                approvedCover,
                                        },
                                    );
                                }}
                                onLoad={() => {
                                    console.log(
                                        '[SalonDetails] Cover image loaded successfully:',
                                        {
                                            salonId:
                                                salon.salonId,
                                        },
                                    );
                                }}
                                onError={(
                                    error,
                                ) => {
                                    console.log(
                                        '[SalonDetails] ❌ COVER LOAD FAILED:',
                                        {
                                            salonId:
                                                salon.salonId,
                                            error:
                                                error?.nativeEvent,
                                            url:
                                                approvedCover,
                                        },
                                    );

                                    setCoverLoadFailed(
                                        true,
                                    );
                                }}
                            />
                        ) : (
                            /**
                             * No approved cover or
                             * approved cover failed.
                             *
                             * Intentionally blank.
                             *
                             * NO fake image.
                             * NO logo fallback.
                             */
                            <View
                                style={
                                    styles.cover
                                }
                            />
                        )}

                        {/* SALON INFO */}

                        <View
                            style={
                                styles.salonInfo
                            }
                        >
                            <Text
                                style={
                                    styles.salonName
                                }
                                numberOfLines={
                                    1
                                }
                            >
                                {
                                    salon.salonName
                                }
                            </Text>

                            {/* RATING */}

                            <View
                                style={
                                    styles.ratingRow
                                }
                            >
                                <View
                                    style={
                                        styles.ratingBadge
                                    }
                                >
                                    <Text
                                        style={
                                            styles.ratingStar
                                        }
                                    >
                                        ★
                                    </Text>

                                    <Text
                                        style={
                                            styles.ratingValue
                                        }
                                    >
                                        {(
                                            salon.averageRating ??
                                            0
                                        ).toFixed(
                                            1,
                                        )}
                                    </Text>
                                </View>

                                <Text
                                    style={
                                        styles.reviewCount
                                    }
                                >
                                    {salon.totalReviews ??
                                        0}{' '}
                                    reviews
                                </Text>
                            </View>

                            {/* ADDRESS */}

                            {!!address && (
                                <Text
                                    style={
                                        styles.address
                                    }
                                    numberOfLines={
                                        2
                                    }
                                >
                                    📍{' '}
                                    {address}
                                </Text>
                            )}

                            {/* CURRENT STATUS */}

                            <View
                                style={
                                    styles.statusRow
                                }
                            >
                                <View
                                    style={[
                                        styles.statusDot,
                                        {
                                            backgroundColor:
                                                isOpen
                                                    ? '#22A06B'
                                                    : '#D64545',
                                        },
                                    ]}
                                />

                                <Text
                                    style={[
                                        styles.statusText,
                                        {
                                            color:
                                                isOpen
                                                    ? '#16834F'
                                                    : '#C03939',
                                        },
                                    ]}
                                >
                                    {isOpen
                                        ? 'Open now'
                                        : 'Closed now'}
                                </Text>

                                {todayHours?.open &&
                                    todayHours?.close && (
                                        <Text
                                            style={
                                                styles.closeText
                                            }
                                        >
                                            •{' '}
                                            {todayHours.open}
                                            {' - '}
                                            {
                                                todayHours.close
                                            }
                                        </Text>
                                    )}
                            </View>

                            {/* FUTURE BOOKING MESSAGE */}

                            {!isOpen && (
                                <Text
                                    style={
                                        styles.futureBookingText
                                    }
                                >
                                    You can still choose a
                                    future date and time.
                                </Text>
                            )}
                        </View>

                        {/* ------------------------------------------------
                            GALLERY
                        ------------------------------------------------ */}

                        {approvedGallery.length >
                            0 && (
                                <View
                                    style={
                                        styles.gallerySection
                                    }
                                >
                                    <View
                                        style={
                                            styles.galleryHeader
                                        }
                                    >
                                        <Text
                                            style={
                                                styles.sectionTitle
                                            }
                                        >
                                            Gallery
                                        </Text>

                                        <Text
                                            style={
                                                styles.galleryCount
                                            }
                                        >
                                            {
                                                approvedGallery.length
                                            }{' '}
                                            {approvedGallery.length ===
                                                1
                                                ? 'photo'
                                                : 'photos'}
                                        </Text>
                                    </View>

                                    <FlatList
                                        data={
                                            approvedGallery
                                        }
                                        horizontal
                                        showsHorizontalScrollIndicator={
                                            false
                                        }
                                        keyExtractor={(
                                            item,
                                            index,
                                        ) =>
                                            item.imageId ||
                                            item.key ||
                                            `gallery-${index}`
                                        }
                                        contentContainerStyle={
                                            styles.galleryList
                                        }
                                        renderItem={({ item, index }) => {
                                            const galleryKey =
                                                item.imageId ||
                                                item.key ||
                                                String(index);

                                            const failed =
                                                galleryLoadFailed[galleryKey];

                                            console.log('[SalonDetails] Rendering gallery image:', {
                                                index,
                                                imageId: item.imageId,
                                                status: item.status,
                                                hasUrl: Boolean(item.objectUrl),
                                            });

                                            if (failed || !item.objectUrl) {
                                                return (
                                                    <View style={styles.galleryImagePlaceholder} />
                                                );
                                            }

                                            return (
                                                <Image
                                                    source={{ uri: item.objectUrl }}
                                                    style={styles.galleryImage}
                                                    resizeMode="cover"

                                                    onLoadStart={() => {
                                                        console.log(
                                                            '[SalonDetails] Gallery image loading:',
                                                            {
                                                                salonId: salon?.salonId,
                                                                imageId: item.imageId,
                                                                index,
                                                            },
                                                        );
                                                    }}

                                                    onLoad={() => {
                                                        console.log(
                                                            '[SalonDetails] Gallery image loaded successfully:',
                                                            {
                                                                salonId: salon?.salonId,
                                                                imageId: item.imageId,
                                                                index,
                                                            },
                                                        );
                                                    }}

                                                    onError={event => {
                                                        console.log(
                                                            '[SalonDetails] ❌ GALLERY IMAGE LOAD FAILED:',
                                                            {
                                                                salonId: salon?.salonId,
                                                                imageId: item.imageId,
                                                                index,
                                                                error: event?.nativeEvent,
                                                                url: item.objectUrl,
                                                            },
                                                        );

                                                        setGalleryLoadFailed(
                                                            previous => ({
                                                                ...previous,
                                                                [galleryKey]: true,
                                                            }),
                                                        );
                                                    }}
                                                />
                                            );
                                        }}
                                    />
                                </View>
                            )}

                        {/* OFFER BANNER */}

                        {normalizedOffer && (
                            <View
                                style={
                                    styles.offerBanner
                                }
                            >
                                <View
                                    style={
                                        styles.offerIcon
                                    }
                                >
                                    <Text
                                        style={
                                            styles.offerIconText
                                        }
                                    >
                                        %
                                    </Text>
                                </View>

                                <View
                                    style={
                                        styles.offerContent
                                    }
                                >
                                    <Text
                                        style={
                                            styles.offerTitle
                                        }
                                        numberOfLines={
                                            2
                                        }
                                    >
                                        {
                                            normalizedOffer.title
                                        }
                                    </Text>

                                    {!!normalizedOffer.description && (
                                        <Text
                                            style={
                                                styles.offerDescription
                                            }
                                            numberOfLines={
                                                2
                                            }
                                        >
                                            {
                                                normalizedOffer.description
                                            }
                                        </Text>
                                    )}

                                    <Text
                                        style={
                                            styles.offerDiscountText
                                        }
                                    >
                                        {getOfferDiscountText()}
                                    </Text>

                                    {normalizedOffer.minimumBookingAmount !==
                                        null &&
                                        normalizedOffer.minimumBookingAmount !==
                                        undefined && (
                                            <Text
                                                style={
                                                    styles.offerMinimum
                                                }
                                            >
                                                Minimum booking:
                                                ₹
                                                {Number(
                                                    normalizedOffer.minimumBookingAmount,
                                                ).toFixed(
                                                    0,
                                                )}
                                            </Text>
                                        )}

                                    {!!offerMessage && (
                                        <Text
                                            style={
                                                styles.offerMessage
                                            }
                                        >
                                            {
                                                offerMessage
                                            }
                                        </Text>
                                    )}
                                </View>
                            </View>
                        )}

                        {/* CATEGORIES */}

                        {categories.length >
                            0 && (
                                <View
                                    style={
                                        styles.categorySection
                                    }
                                >
                                    <Text
                                        style={
                                            styles.sectionTitle
                                        }
                                    >
                                        Services available
                                    </Text>

                                    <View
                                        style={
                                            styles.categoryRow
                                        }
                                    >
                                        {categories.map(
                                            category => (
                                                <View
                                                    key={
                                                        category
                                                    }
                                                    style={
                                                        styles.categoryChip
                                                    }
                                                >
                                                    <Text
                                                        style={
                                                            styles.categoryText
                                                        }
                                                    >
                                                        {
                                                            category
                                                        }
                                                    </Text>
                                                </View>
                                            ),
                                        )}
                                    </View>
                                </View>
                            )}

                        {/* SERVICES HEADER */}

                        <View
                            style={
                                styles.servicesHeader
                            }
                        >
                            <View>
                                <Text
                                    style={
                                        styles.sectionTitle
                                    }
                                >
                                    Services
                                </Text>

                                <Text
                                    style={
                                        styles.serviceCount
                                    }
                                >
                                    {
                                        services.length
                                    }{' '}
                                    services
                                    available
                                </Text>
                            </View>
                        </View>
                    </>
                }
                renderItem={({
                    item,
                }) => {
                    const selected =
                        selectedServices.some(
                            service =>
                                service.serviceId ===
                                item.serviceId,
                        );

                    const eligible =
                        isServiceEligibleForOffer(
                            item,
                        );

                    const originalPrice =
                        Number(
                            item.price || 0,
                        );

                    const displayPrice =
                        selected
                            ? getServiceDisplayPrice(
                                item,
                            )
                            : originalPrice;

                    const hasDiscount =
                        offerApplied &&
                        eligible &&
                        displayPrice <
                        originalPrice;

                    return (
                        <View
                            style={[
                                styles.serviceCard,
                                selected &&
                                styles.selectedServiceCard,
                                offerApplied &&
                                eligible &&
                                styles.offerEligibleCard,
                            ]}
                        >
                            <View
                                style={
                                    styles.serviceInfo
                                }
                            >
                                <View
                                    style={
                                        styles.serviceNameRow
                                    }
                                >
                                    <Text
                                        style={
                                            styles.serviceName
                                        }
                                        numberOfLines={
                                            1
                                        }
                                    >
                                        {
                                            item.name
                                        }
                                    </Text>

                                    {item.popular && (
                                        <View
                                            style={
                                                styles.popularBadge
                                            }
                                        >
                                            <Text
                                                style={
                                                    styles.popularText
                                                }
                                            >
                                                Popular
                                            </Text>
                                        </View>
                                    )}
                                </View>

                                {!!item.category && (
                                    <Text
                                        style={
                                            styles.serviceCategory
                                        }
                                    >
                                        {
                                            item.category
                                        }
                                    </Text>
                                )}

                                {!!item.description && (
                                    <Text
                                        style={
                                            styles.description
                                        }
                                        numberOfLines={
                                            2
                                        }
                                    >
                                        {
                                            item.description
                                        }
                                    </Text>
                                )}

                                <View
                                    style={
                                        styles.serviceMeta
                                    }
                                >
                                    <View
                                        style={
                                            styles.servicePriceContainer
                                        }
                                    >
                                        {hasDiscount && (
                                            <Text
                                                style={
                                                    styles.originalPrice
                                                }
                                            >
                                                ₹
                                                {originalPrice.toFixed(
                                                    0,
                                                )}
                                            </Text>
                                        )}

                                        <Text
                                            style={[
                                                styles.price,
                                                hasDiscount &&
                                                styles.discountedPrice,
                                            ]}
                                        >
                                            ₹
                                            {displayPrice.toFixed(
                                                0,
                                            )}
                                        </Text>
                                    </View>

                                    <View
                                        style={
                                            styles.metaDot
                                        }
                                    />

                                    <Text
                                        style={
                                            styles.duration
                                        }
                                    >
                                        {
                                            item.duration
                                        }{' '}
                                        mins
                                    </Text>
                                </View>

                                {offerApplied &&
                                    eligible && (
                                        <Text
                                            style={
                                                styles.offerAppliedText
                                            }
                                        >
                                            ✓ Offer applied
                                        </Text>
                                    )}

                                {normalizedOffer &&
                                    isOfferValid &&
                                    !eligible && (
                                        <Text
                                            style={
                                                styles.notEligibleText
                                            }
                                        >
                                            Offer not applicable
                                        </Text>
                                    )}
                            </View>

                            <TouchableOpacity
                                style={[
                                    styles.addButton,
                                    selected &&
                                    styles.selectedAddButton,
                                ]}
                                onPress={() =>
                                    toggleService(
                                        item,
                                    )
                                }
                                activeOpacity={
                                    0.8
                                }
                            >
                                <Text
                                    style={[
                                        styles.addButtonText,
                                        selected &&
                                        styles.selectedAddButtonText,
                                    ]}
                                >
                                    {selected
                                        ? '✓'
                                        : '+'}
                                </Text>
                            </TouchableOpacity>
                        </View>
                    );
                }}
                ListEmptyComponent={
                    <View
                        style={
                            styles.emptyServices
                        }
                    >
                        <Text
                            style={
                                styles.emptyTitle
                            }
                        >
                            No services available
                        </Text>

                        <Text
                            style={
                                styles.emptyText
                            }
                        >
                            This salon has not
                            added any services
                            yet.
                        </Text>
                    </View>
                }
            />

            {/* ------------------------------------------------
                BOTTOM BOOKING BAR
            ------------------------------------------------ */}

            {selectedServices.length >
                0 && (
                    <View
                        style={
                            styles.bottomBar
                        }
                    >
                        <View
                            style={
                                styles.bottomInfo
                            }
                        >
                            <Text
                                style={
                                    styles.selectedText
                                }
                            >
                                {
                                    selectedServices.length
                                }{' '}
                                {selectedServices.length ===
                                    1
                                    ? 'service'
                                    : 'services'}
                            </Text>

                            {offerApplied && (
                                <Text
                                    style={
                                        styles.bottomOriginalPrice
                                    }
                                >
                                    ₹
                                    {subtotal.toFixed(
                                        0,
                                    )}
                                </Text>
                            )}

                            <Text
                                style={
                                    styles.totalPrice
                                }
                            >
                                ₹
                                {discountedServicesTotal.toFixed(
                                    0,
                                )}
                            </Text>

                            {offerApplied && (
                                <Text
                                    style={
                                        styles.bottomSavings
                                    }
                                >
                                    Save ₹
                                    {discountAmount.toFixed(
                                        0,
                                    )}
                                </Text>
                            )}

                            {!offerApplied &&
                                normalizedOffer &&
                                !minimumBookingAmountMet && (
                                    <Text
                                        style={
                                            styles.minimumAmountWarning
                                        }
                                    >
                                        Add more for offer
                                    </Text>
                                )}

                            <Text
                                style={
                                    styles.totalDuration
                                }
                            >
                                {
                                    totalDuration
                                }{' '}
                                mins
                            </Text>
                        </View>

                        {/* 
                     * IMPORTANT:
                     *
                     * Do NOT disable this button when the
                     * salon is currently closed.
                     *
                     * A salon may be closed now but open
                     * tomorrow.
                     *
                     * BookingDateTimeScreen will determine
                     * which future dates/times are valid.
                     */}
                        <TouchableOpacity
                            style={
                                styles.continueButton
                            }
                            activeOpacity={
                                0.85
                            }
                            onPress={() => {
                                if (
                                    !currentUser?.userId
                                ) {
                                    Alert.alert(
                                        'Login required',
                                        'Please login to continue booking.',
                                    );

                                    return;
                                }

                                if (
                                    !salon?.salonId
                                ) {
                                    Alert.alert(
                                        'Salon unavailable',
                                        'Salon information is missing. Please try again.',
                                    );

                                    return;
                                }

                                if (
                                    selectedServices.length ===
                                    0
                                ) {
                                    Alert.alert(
                                        'Select a service',
                                        'Please select at least one service before continuing.',
                                    );

                                    return;
                                }

                                /**
                                 * ------------------------------------------------
                                 * SEND EVERYTHING TO BOOKING DATETIME
                                 * ------------------------------------------------
                                 */
                                const params = {
                                    salonId:
                                        salon.salonId,

                                    /**
                                     * Full salon object.
                                     *
                                     * This includes business hours
                                     * and approved media.
                                     */
                                    salon,

                                    salonName:
                                        salon.salonName,

                                    customerUserId:
                                        currentUser.userId,

                                    services:
                                        selectedServices,

                                    /**
                                     * Offer object.
                                     */
                                    offer:
                                        normalizedOffer ||
                                        undefined,

                                    /**
                                     * Explicit offer ID.
                                     */
                                    offerId:
                                        routeOfferId ||
                                        normalizedOffer?.offerId ||
                                        undefined,

                                    /**
                                     * Display pricing.
                                     *
                                     * Backend remains
                                     * authoritative.
                                     */
                                    subtotal,

                                    discountAmount,

                                    totalPrice:
                                        discountedServicesTotal,

                                    offerApplied,

                                    /**
                                     * Useful information for
                                     * BookingDateTimeScreen.
                                     */
                                    totalDuration,

                                    /**
                                     * Keep original business
                                     * hours explicitly available
                                     * as well.
                                     */
                                    businessHours:
                                        salon.businessHours ??
                                        {
                                            MONDAY:
                                                (
                                                    salon as any
                                                ).MONDAY,

                                            TUESDAY:
                                                (
                                                    salon as any
                                                ).TUESDAY,

                                            WEDNESDAY:
                                                (
                                                    salon as any
                                                ).WEDNESDAY,

                                            THURSDAY:
                                                (
                                                    salon as any
                                                ).THURSDAY,

                                            FRIDAY:
                                                (
                                                    salon as any
                                                ).FRIDAY,

                                            SATURDAY:
                                                (
                                                    salon as any
                                                ).SATURDAY,

                                            SUNDAY:
                                                (
                                                    salon as any
                                                ).SUNDAY,
                                        },
                                };

                                console.log(
                                    '======================================',
                                );

                                console.log(
                                    'SENDING TO BOOKING DATETIME',
                                );

                                console.log(
                                    'Salon ID:',
                                    params.salonId,
                                );

                                console.log(
                                    'Salon Name:',
                                    params.salonName,
                                );

                                console.log(
                                    'Business Hours:',
                                    params.businessHours,
                                );

                                console.log(
                                    'Selected services:',
                                    params.services,
                                );

                                console.log(
                                    'Total duration:',
                                    params.totalDuration,
                                );

                                console.log(
                                    'Offer:',
                                    params.offer,
                                );

                                console.log(
                                    'Offer ID:',
                                    params.offerId,
                                );

                                console.log(
                                    'Subtotal:',
                                    params.subtotal,
                                );

                                console.log(
                                    'Discount:',
                                    params.discountAmount,
                                );

                                console.log(
                                    'Discounted total:',
                                    params.totalPrice,
                                );

                                console.log(
                                    'Offer applied:',
                                    params.offerApplied,
                                );

                                console.log(
                                    'Currently open:',
                                    isOpen,
                                );

                                console.log(
                                    '======================================',
                                );

                                navigation.navigate(
                                    'BookingDateTime',
                                    params,
                                );
                            }}
                        >
                            <Text
                                style={
                                    styles.continueText
                                }
                            >
                                Continue
                            </Text>

                            <Text
                                style={
                                    styles.continueArrow
                                }
                            >
                                →
                            </Text>
                        </TouchableOpacity>
                    </View>
                )}
        </SafeAreaView>
    );
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#F5F6FA',
    },

    loadingContainer: {
        flex: 1,
        backgroundColor: '#F5F6FA',
        alignItems: 'center',
        justifyContent: 'center',
        paddingHorizontal: 30,
    },

    loadingText: {
        marginTop: 12,
        fontSize: 15,
        color: '#666',
    },

    errorTitle: {
        fontSize: 20,
        fontWeight: '700',
        color: '#111',
    },

    errorText: {
        marginTop: 8,
        textAlign: 'center',
        color: '#777',
        fontSize: 14,
    },

    retryButton: {
        marginTop: 20,
        backgroundColor: PRIMARY,
        paddingHorizontal: 25,
        paddingVertical: 12,
        borderRadius: 22,
    },

    retryText: {
        color: '#FFF',
        fontWeight: '700',
    },

    header: {
        height: 58,
        backgroundColor: '#FFF',
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
        paddingHorizontal: 15,
    },

    headerButton: {
        width: 40,
        height: 40,
        borderRadius: 20,
        alignItems: 'center',
        justifyContent: 'center',
    },

    back: {
        fontSize: 38,
        lineHeight: 40,
        color: '#222',
        fontWeight: '300',
    },

    headerTitle: {
        flex: 1,
        textAlign: 'center',
        fontSize: 17,
        fontWeight: '700',
        color: '#111',
        marginHorizontal: 10,
    },

    favorite: {
        fontSize: 16,
        color: '#333',
        lineHeight: 34,
    },

    favoriteActive: {
        color: '#E53935',
    },

    /**
     * ----------------------------------------------------
     * COVER
     * ----------------------------------------------------
     */
    cover: {
        width: '100%',
        height: 220,
        backgroundColor: '#E8E8E8',
    },

    salonInfo: {
        backgroundColor: '#FFF',
        paddingHorizontal: 18,
        paddingTop: 16,
        paddingBottom: 17,
    },

    salonName: {
        fontSize: 22,
        fontWeight: '800',
        color: '#111',
    },

    ratingRow: {
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 9,
    },

    ratingBadge: {
        flexDirection: 'row',
        alignItems: 'center',
        backgroundColor: '#FFF4D9',
        borderRadius: 8,
        paddingHorizontal: 8,
        paddingVertical: 4,
    },

    ratingStar: {
        color: '#F4A623',
        fontSize: 13,
        marginRight: 4,
    },

    ratingValue: {
        color: '#222',
        fontSize: 13,
        fontWeight: '800',
    },

    reviewCount: {
        marginLeft: 8,
        fontSize: 13,
        color: '#777',
    },

    address: {
        marginTop: 10,
        color: '#666',
        fontSize: 13,
        lineHeight: 19,
    },

    statusRow: {
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 10,
    },

    statusDot: {
        width: 8,
        height: 8,
        borderRadius: 4,
        marginRight: 7,
    },

    statusText: {
        fontSize: 13,
        fontWeight: '800',
    },

    closeText: {
        marginLeft: 6,
        fontSize: 13,
        color: '#777',
    },

    futureBookingText: {
        marginTop: 7,
        color: PRIMARY,
        fontSize: 12,
        fontWeight: '600',
    },

    /**
     * ----------------------------------------------------
     * GALLERY
     * ----------------------------------------------------
     */

    gallerySection: {
        backgroundColor: '#FFF',
        marginTop: 8,
        paddingTop: 16,
        paddingBottom: 17,
    },

    galleryHeader: {
        paddingHorizontal: 18,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
    },

    galleryCount: {
        fontSize: 12,
        color: '#888',
        fontWeight: '600',
    },

    galleryList: {
        paddingLeft: 18,
        paddingRight: 6,
        marginTop: 12,
    },

    galleryImage: {
        width: 170,
        height: 125,
        borderRadius: 13,
        backgroundColor: '#E8E8E8',
        marginRight: 10,
    },

    galleryImagePlaceholder: {
        width: 170,
        height: 125,
        borderRadius: 13,
        backgroundColor: '#E8E8E8',
        marginRight: 10,
    },

    /**
     * ----------------------------------------------------
     * OFFER
     * ----------------------------------------------------
     */

    offerBanner: {
        marginTop: 8,
        marginHorizontal: 0,
        paddingHorizontal: 18,
        paddingVertical: 16,
        backgroundColor: '#EAF8F3',
        borderTopWidth: 1,
        borderBottomWidth: 1,
        borderColor: '#B9E5D5',
        flexDirection: 'row',
        alignItems: 'center',
    },

    offerIcon: {
        width: 42,
        height: 42,
        borderRadius: 21,
        backgroundColor: PRIMARY,
        alignItems: 'center',
        justifyContent: 'center',
        marginRight: 12,
    },

    offerIconText: {
        color: '#FFF',
        fontSize: 20,
        fontWeight: '800',
    },

    offerContent: {
        flex: 1,
    },

    offerTitle: {
        fontSize: 16,
        fontWeight: '800',
        color: '#176B53',
    },

    offerDescription: {
        marginTop: 3,
        color: '#477568',
        fontSize: 12,
        lineHeight: 17,
    },

    offerDiscountText: {
        marginTop: 5,
        color: PRIMARY,
        fontSize: 14,
        fontWeight: '800',
    },

    offerMinimum: {
        marginTop: 3,
        color: '#6B7F78',
        fontSize: 11,
    },

    offerMessage: {
        marginTop: 5,
        color: '#477568',
        fontSize: 11,
        fontWeight: '600',
    },

    /**
     * ----------------------------------------------------
     * CATEGORIES
     * ----------------------------------------------------
     */

    categorySection: {
        backgroundColor: '#FFF',
        marginTop: 8,
        paddingHorizontal: 18,
        paddingVertical: 17,
    },

    sectionTitle: {
        fontSize: 18,
        fontWeight: '800',
        color: '#111',
    },

    categoryRow: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        marginTop: 11,
    },

    categoryChip: {
        backgroundColor: '#EAF6F3',
        borderRadius: 18,
        paddingHorizontal: 13,
        paddingVertical: 7,
        marginRight: 7,
        marginBottom: 7,
    },

    categoryText: {
        color: PRIMARY,
        fontSize: 12,
        fontWeight: '700',
    },

    /**
     * ----------------------------------------------------
     * SERVICES
     * ----------------------------------------------------
     */

    servicesHeader: {
        backgroundColor: '#F5F6FA',
        paddingHorizontal: 18,
        paddingTop: 20,
        paddingBottom: 8,
    },

    serviceCount: {
        marginTop: 3,
        fontSize: 12,
        color: '#888',
    },

    serviceCard: {
        marginHorizontal: 15,
        marginVertical: 6,
        padding: 15,
        backgroundColor: '#FFF',
        borderRadius: 15,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
        borderWidth: 1,
        borderColor: '#F0F0F0',
    },

    selectedServiceCard: {
        borderColor: PRIMARY,
        backgroundColor: '#F7FCFB',
    },

    offerEligibleCard: {
        borderColor: '#B9E5D5',
    },

    serviceInfo: {
        flex: 1,
        paddingRight: 12,
    },

    serviceNameRow: {
        flexDirection: 'row',
        alignItems: 'center',
    },

    serviceName: {
        flexShrink: 1,
        fontSize: 16,
        fontWeight: '700',
        color: '#111',
    },

    popularBadge: {
        marginLeft: 7,
        backgroundColor: '#FFF2D8',
        paddingHorizontal: 7,
        paddingVertical: 3,
        borderRadius: 8,
    },

    popularText: {
        color: '#B87900',
        fontSize: 9,
        fontWeight: '800',
    },

    serviceCategory: {
        marginTop: 4,
        fontSize: 11,
        color: PRIMARY,
        fontWeight: '600',
    },

    description: {
        marginTop: 5,
        color: '#888',
        fontSize: 11,
        lineHeight: 16,
    },

    serviceMeta: {
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 8,
    },

    servicePriceContainer: {
        alignItems: 'flex-start',
    },

    originalPrice: {
        color: '#999',
        fontSize: 12,
        textDecorationLine:
            'line-through',
    },

    price: {
        color: PRIMARY,
        fontSize: 16,
        fontWeight: '800',
    },

    discountedPrice: {
        color: PRIMARY,
    },

    metaDot: {
        width: 3,
        height: 3,
        borderRadius: 2,
        backgroundColor: '#AAA',
        marginHorizontal: 8,
    },

    duration: {
        color: '#777',
        fontSize: 12,
    },

    offerAppliedText: {
        marginTop: 5,
        color: PRIMARY,
        fontSize: 11,
        fontWeight: '700',
    },

    notEligibleText: {
        marginTop: 5,
        color: '#999',
        fontSize: 10,
    },

    addButton: {
        width: 38,
        height: 38,
        borderRadius: 19,
        backgroundColor: PRIMARY,
        alignItems: 'center',
        justifyContent: 'center',
    },

    selectedAddButton: {
        backgroundColor: '#E6F5F1',
        borderWidth: 1,
        borderColor: PRIMARY,
    },

    addButtonText: {
        color: '#FFF',
        fontSize: 24,
        fontWeight: '500',
        lineHeight: 27,
    },

    selectedAddButtonText: {
        color: PRIMARY,
        fontSize: 18,
        fontWeight: '800',
    },

    emptyServices: {
        paddingVertical: 50,
        alignItems: 'center',
        paddingHorizontal: 30,
    },

    emptyTitle: {
        fontSize: 17,
        fontWeight: '700',
        color: '#222',
    },

    emptyText: {
        marginTop: 7,
        textAlign: 'center',
        fontSize: 13,
        color: '#888',
    },

    /**
     * ----------------------------------------------------
     * BOTTOM BOOKING BAR
     * ----------------------------------------------------
     */

    bottomBar: {
        position: 'absolute',
        left: 0,
        right: 0,
        bottom: 0,
        backgroundColor: '#FFF',
        paddingHorizontal: 17,
        paddingTop: 13,
        paddingBottom: 14,
        borderTopWidth: 1,
        borderTopColor: '#E8E8E8',
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
    },

    bottomInfo: {
        flex: 1,
    },

    selectedText: {
        fontSize: 12,
        color: '#777',
        fontWeight: '600',
    },

    bottomOriginalPrice: {
        marginTop: 2,
        color: '#999',
        fontSize: 12,
        textDecorationLine:
            'line-through',
    },

    totalPrice: {
        marginTop: 2,
        fontSize: 20,
        color: PRIMARY,
        fontWeight: '800',
    },

    bottomSavings: {
        marginTop: 1,
        color: '#16845E',
        fontSize: 11,
        fontWeight: '700',
    },

    minimumAmountWarning: {
        marginTop: 2,
        color: '#B87900',
        fontSize: 10,
        fontWeight: '700',
    },

    totalDuration: {
        marginTop: 1,
        fontSize: 11,
        color: '#888',
    },

    continueButton: {
        minWidth: 125,
        backgroundColor: PRIMARY,
        paddingHorizontal: 18,
        paddingVertical: 13,
        borderRadius: 25,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
    },

    continueText: {
        color: '#FFF',
        fontSize: 14,
        fontWeight: '800',
    },

    continueArrow: {
        color: '#FFF',
        fontSize: 18,
        marginLeft: 7,
    },
});